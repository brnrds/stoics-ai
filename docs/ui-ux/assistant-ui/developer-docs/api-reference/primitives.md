# Primitives API Reference
URL: /docs/api-reference/primitives

Composable React primitives for assistant-ui chat UIs: Thread, Composer, Message, BranchPicker, ActionBar, and the parts that build threads, message lists, attachments, and editing flows.

Primitives are the low-level React components behind assistant-ui interfaces. They provide accessible behavior, state wiring, and composition points while leaving layout and styling under your control.

## [Pages](#pages)

- href

  /docs/api-reference/primitives/action-bar

ActionBarPrimitiveComposable message action controls for copy, edit, reload, speech, and feedback in assistant-ui chat interfaces.

- href

  /docs/api-reference/primitives/action-bar-more

ActionBarMorePrimitiveOverflow menu primitives for grouping secondary assistant message actions in a custom React UI.

- href

  /docs/api-reference/primitives/assistant-if

AuiIfConditional rendering primitive for showing React UI from assistant-ui thread, message, composer, and runtime state.

- href

  /docs/api-reference/primitives/assistant-modal

AssistantModalPrimitiveFloating assistant modal primitives for building support chat, copilot, and embedded assistant experiences.

- href

  /docs/api-reference/primitives/attachment

AttachmentPrimitiveAttachment primitives for rendering file previews, names, thumbnails, and remove controls in assistant-ui messages and composers.

- href

  /docs/api-reference/primitives/branch-picker

BranchPickerPrimitiveBranch picker primitives for navigating regenerated assistant responses and alternate message paths inside a chat thread.

- href

  /docs/api-reference/primitives/chain-of-thought

ChainOfThoughtPrimitiveChain of thought primitives for rendering assistant reasoning, step lists, and collapsible disclosure UI in message content.

- href

  /docs/api-reference/primitives/composer

ComposerPrimitiveComposable input primitives for assistant-ui prompts, send controls, cancellation, attachments, and composer state.

- href

  /docs/api-reference/primitives/error

ErrorPrimitiveError primitives for rendering assistant-ui runtime, thread, and message failures inside custom chat interfaces.

- href

  /docs/api-reference/primitives/message

MessagePrimitiveMessage primitives for rendering assistant and user turns, message parts, attachments, actions, editing, and branch controls.

- href

  /docs/api-reference/primitives/message-part

MessagePartPrimitiveMessage part primitives for rendering text, tool calls, data parts, reasoning, source content, and custom assistant output.

- href

  /docs/api-reference/primitives/queue-item

QueueItemPrimitiveQueue item primitives for rendering pending assistant-ui thread operations, optimistic work, and runtime queue state.

- href

  /docs/api-reference/primitives/selection-toolbar

SelectionToolbarPrimitiveSelection toolbar primitives for quote, copy, and contextual actions on selected chat text.

- href

  /docs/api-reference/primitives/suggestion

SuggestionPrimitiveSuggestion primitives for rendering starter prompts, follow-up actions, and composer suggestions in assistant-ui threads.

- href

  /docs/api-reference/primitives/thread

ThreadPrimitiveThread primitives for rendering chat transcripts, message lists, viewport state, suggestions, and composers in assistant-ui.

- href

  /docs/api-reference/primitives/thread-list

ThreadListPrimitiveThread list primitives for rendering conversation navigation, new thread actions, and custom assistant sidebars.

- href

  /docs/api-reference/primitives/thread-list-item

ThreadListItemPrimitiveThread list item primitives for rendering selectable conversation rows with titles, archive controls, delete actions, and menus.

- href

  /docs/api-reference/primitives/thread-list-item-more

ThreadListItemMorePrimitiveOverflow menu primitives for secondary thread list item actions in custom assistant-ui sidebars.